[options]
# This isolated format test verifies the repository database hash by mounting
# it locally. Production repositories use the signed channel trust chain.
RootDir = /sdk
DBPath = /sdk/var/lib/pacman/
CacheDir = /sdk/var/cache/pacman/pkg/
Architecture = auto vita
SigLevel = Never

[vitasdk-test]
Server = file:///repo
